declared-services:
  language-translator-service:
    label: language_translator
    plan: standard
applications:
- services:
  - language-translator-service
  name: language-translator-demo
  command: npm start
  path: .
  memory: 512M
